/* import { createSlice } from '@reduxjs/toolkit';

// Initial State
const initialState = {};

// Slice
const postForm = createSlice({
	name: 'redux-racstagram/postForm',
	initialState,
	reducers: {},
	extraReducers: {},
});

export default postForm.reducer;

// actionCreator
export const {} = postForm.actions;

// Async */
